{
  "private": true,
  "type": "module",
  "scripts": {
    "dev:packages": "turbo run dev --filter=!docs",
    "dev:docs": "turbo run dev --filter=docs",
    "build": "turbo run build --filter=!docs",
    "build-demo": "turbo run build --filter=web",
    "build-packages": "turbo run build --filter=\"./packages/*\"",
    "test": "vitest",
    "test:watch": "vitest --watch",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.2",
    "eslint": "^8.57.0",
    "jsdom": "^28.1.0",
    "prettier": "^3.2.5",
    "tsdown": "^0.20.3",
    "turbo": "^2.8.10",
    "typescript": "^5.9.3",
    "vite-tsconfig-paths": "^6.1.1",
    "vitest": "^4.0.18"
  },
  "name": "with-vite-react",
  "packageManager": "yarn@1.22.22",
  "workspaces": [
    "apps/*",
    "packages/*",
    "configs/*",
    "data/*"
  ],
  "resolutions": {
    "react": "18.3.1",
    "react-dom": "18.3.1",
    "@types/react": "18.3.28",
    "@types/react-dom": "18.3.7"
  }
}
